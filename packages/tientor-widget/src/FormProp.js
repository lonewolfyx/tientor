export default {
    // 表单数据对象
    model: {},
    // 表单的布局方式
    layout: '',
    // 表单控件的尺寸
    size: '',
    // 是否禁用表单
    disabled: '',
    // 表单项校验规则
    rules: '',
    // 是否开启自动标签宽度，仅在 `layout="horizontal"` 下生效。
    autoLabelWidth: false,
    // 表单控件 `id` 的前缀
    id: '',
    // 验证失败后滚动到第一个错误字段
    scrollToFirstError: false
}